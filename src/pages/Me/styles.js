import styled from "styled-components";
import { icomoon } from "styled-icons";

export const ProfileLink = styled.li`
  margin-right: 0.5rem;
  margin-bottom: 0.5rem;
  display: inline-block;
  font-size: 32px;
`;

export const StackIcon = styled(icomoon.Stack) `
  color: red;
  width: 50px;
  height: auto;
`;

